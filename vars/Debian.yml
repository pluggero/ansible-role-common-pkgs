---
common_pkgs_dep_pkgs:
  - pipx

# common apt packages to install
common_pkgs_role:
  - python3-venv
  - bc
  - hostapd # mobile access point
  - python3-qrcode # mobile acces point
  - adb

# common pip packages to install
common_pkgs_pip_role:
  - frida-tools
  - objection

# common golang packages to install
common_pkgs_go_role:

# common git repos to install
common_pkgs_git_repos_role:
